# Changelog

把 `progress.md` 裡上一輪的內容整段搬到這裡，新的在最上面。這裡不寫功能規格。

## 2026-09-25（應用程式 id、T Reader、GitHub）

## 這次做了什麼

- 應用程式 id 改為 `riverine.studio.epub`。`MainActivity` 的套件路徑一併改了。通知頻道 id 改為 `riverine.studio.epub.speech`。
- 應用程式名稱改為 `T Reader`（啟動圖示名稱、Flutter 視窗標題、說明文件標題）。Google Drive 同步資料夾仍叫 `EPUB Reader`。
- 新增 `scripts/push-github.sh`：提交並推到 `origin`。加上 `--release` 會建置 release APK，上傳到 GitHub Release（標籤用 `pubspec.yaml` 的版本，同一版本會換掉舊的 APK）。
- 專案放到 GitHub：`Hocti/epub-reader`。

書櫃與 Drive 教學：

- 新增 `docs/google-drive-setup.md`：繁中的 Google Drive 設定教學（SHA-1、Cloud 專案、Drive API、同意畫面、Android 用戶端、常見錯誤）。
- 書櫃上方一行不再左右捲動：搜尋改成左下角的圓形浮動按鈕；篩選按鈕只有圖示，不是預設時填色；排序拿掉「修改日期」（加入時間），按鈕和讀設定的那行留在註解裡，舊設定回到「最近閱讀」。
- 每本書在時間前面顯示狀態小框：閱讀中、未開始、已完結（填色）、封存。未開始不再寫「尚未閱讀」。
- 已完結不一定要 100%：`book_text.dart` 的 `endChapter` 在最後 10% 的章節（按章數）找章名含致謝、版權、註釋、譯者（含簡體和「注釋」）的第一章，讀到那一章就算完結。結果存在 `book_meta_v1` 的 `end`；舊快取沒有這欄，會重讀一次書（章節份量保留）。
- `flutter analyze` 沒有問題，53 個測試全部通過。新增完結位置和狀態的測試。沒有建 APK。

## 下一步

- Google Cloud 的 Android OAuth 用戶端要改成套件名稱 `riverine.studio.epub`，否則 Google Drive 登入會失敗（status 10）。步驟見 `docs/google-drive-setup.md`。
- 實機看書櫃上方一行在窄螢幕是否放得下，浮動搜尋鈕有沒有擋到最後一本書。

## 2026-09-25（書櫃篩選、閱讀資料同步、劃線標籤圖片）

## 這次做了什麼

書櫃：

- 「顯示封存」改成一顆篩選按鈕，寫著目前的篩選，按下出現選單：書櫃（預設）、閱讀中、未開始、已完結、封存、全部。閱讀中、未開始、已完結不含封存的書。舊設定 `showArchived` 會變成「封存」。

閱讀資料與同步：

- 新增 `lib/src/book_data.dart`：每本書的進度、劃線、標籤存成一個以書名命名的 JSON 檔，全部在同一個目錄。預設在 app support 的 `book-data/`，設定頁可以改目錄（舊檔搬過去並合併）。
- 以前存在 `reader_v1` 的劃線和標籤，第一次打開那本書時搬進檔案。
- 新增 `lib/src/drive_sync.dart`：`google_sign_in` 6.3.0 加 Drive v3 REST，範圍 `drive.file`，資料夾 `EPUB Reader`。開 app、回到 app、離開 app、本機改動後 8 秒、按「立即同步」時同步。只下載 Drive 上改過的檔，只上傳本機改過的檔。
- 合併規則：進度用 `lastRead` 較晚的一邊；劃線和標籤兩邊都留；刪掉的記在 `removed`，不會被另一邊加回來；標籤名稱用較晚改的。

劃線、標籤、圖片：

- 劃線記下建立時間。目錄抽屜改成四個分頁：目錄、劃線、標籤、圖片。劃線新的在前，寫日期時間和章名，有刪除鍵。標籤也寫日期時間。
- 選取範圍碰到已有劃線時，「劃線」變成「移除劃線」，同一段不會劃兩次。
- 圖片分頁列出書中每張不同的圖（縮圖、章名），點了跳到第一次出現的地方（`reader.goImage`）。
- 下方列拿掉「劃線」。

閱讀頁：

- 最底中間顯示這一章的頁數／總頁數。
- 百分比捲軸上方加「最頭」「上章」「下章」。
- 下方列改成：目錄、版面、光度、方向、朗讀。光度打開一條可拖的捲軸，另有「跟隨系統」。方向可選自動旋轉、直向、橫向、反向橫向，整個 app 生效並記住。
- 畫面寬過 800px 時，版面多一項「欄數」：單欄或雙欄（只用在橫排翻頁）。

設定：

- 閱讀時螢幕不休眠（預設關）。
- 開 app 時打開上次正在讀的書（預設開）：從閱讀頁關掉 app，下次直接回到那本書。

Android：

- `MainActivity` 新增 `epub_reader/screen` 頻道：`brightness`、`systemBrightness`、`keepOn`。

檢查：

- `flutter analyze` 沒有問題，52 個測試全部通過。新增測試：每本書一個檔、合併規則、劃線重疊、檔名、篩選、圖片順序、欄數設定。
- Samsung SSD 沒有掛上，所以沒有建 APK。Kotlin、Google 登入和 WebView 裡的雙欄、跳到圖片都未在實機上試過。

## 下一步

- Google Drive 要先在 Google Cloud Console 登記才能登入：開一個專案，啟用 Google Drive API，OAuth 同意畫面加上 `drive.file` 範圍，再建一個 Android OAuth 用戶端，套件名稱 `app.epubreader.epub_reader`，SHA-1 用簽署金鑰的。現在 debug 用 `~/.android/debug.keystore`，SHA-1 是 `A9:A7:73:87:FC:A0:B8:86:06:B8:36:09:D8:B7:AD:DF:38:D8:4D:AD`。之後換成 `android/key.properties` 的金鑰時，要再加那把的 SHA-1。同意畫面在「測試」狀態時，要把自己的 Google 帳號加進測試使用者。
- SSD 掛上後跑 `scripts/build-android.sh`，在實機上試：兩台裝置同步、篩選、雙欄（平板橫放）、光度捲軸、方向、螢幕不休眠、開 app 回到上次的書、圖片分頁跳轉、移除劃線。

## 2026-09-25（字標色、上下邊界、直排翻頁、從別的 app 打開）

## 這次做了什麼

朗讀：

- 這一句裡正在讀的字另外標色（`reader.word`，用 `flutter_tts` 回報的字位置）。淺色主題琥珀色，深色主題淺藍色，電子紙主題是整句的反白再反過來。
- 讀到的字已在下一頁時立刻翻頁；捲動模式就捲到那個字。
- Dart 的句子文字會合併空白、去掉全形空白、去頭尾，JS 用同一套規則把字位置對回頁面上的字。

版面與翻頁：

- 邊界分成左右（12、24、40 px）和上下（在原本上 24、下 12 px 之外再加 0、12、32 px，預設 12）。舊設定沒有上下邊界時用標準。
- 直排翻頁重寫：量每一行的位置，一頁放到下一行會越過左邊界為止，不再用固定頁寬，所以不會切到半行。兩邊用遮罩蓋住隔壁頁的行。
- 橫排找一句在哪一頁時，改成往下取整。原本四捨五入，一句從一行中間偏右開始時，會被算到下一頁。
- 捲動模式捲動時，選取的兩顆圓鈕重新定位，跟著字走。

面板與返回鍵：

- 目錄點右邊空白處關閉；點查點上半部關閉。版面原本就會。
- 系統返回鍵先關最上面一層：點查、面板、朗讀選單、選取、百分比捲軸，都關了才回書櫃。書櫃的搜尋列也一樣。

書櫃：

- 右上加搜尋圖示，打開一條輸入列，邊打邊篩書名或檔名，不分大小寫、不計空白。
- 列表每本左邊加一張 48 × 72 的小封面。
- 從別的 app 打開 `.epub`：`AndroidManifest.xml` 加了 `VIEW` 的 intent filter，`MainActivity` 加了 `epub_reader/open` 頻道。讀得到原檔就用原檔路徑，讀不到就複製到 app 的 `files/opened/`。這些檔案記在 `library_v1` 的 `openedFiles`，不限於設定的目錄。打開後直接進入閱讀。

檢查：

- 在瀏覽器用 `電玩的本質` 第一章（Pixel 7 大小）量過：直排 53 頁，沒有一行被切或超出邊界；橫排 364 句都落在自己第一行所在的那一頁；字標色和跨頁翻頁正常。
- 新增測試：開啟過的檔案、書櫃搜尋、只有開啟檔案時的空書櫃訊息、上下邊界設定。`flutter analyze` 沒有問題，46 個測試全部通過。
- Samsung SSD 沒有掛上，所以沒有建 APK，Kotlin 和 manifest 的改動未經編譯。

## 下一步

- SSD 掛上後跑 `scripts/build-android.sh`，在實機上試：從檔案管理員打開 epub（包括只給 content 連結的 app）、字標色與跨頁翻頁、直排翻頁、捲動模式的圓鈕、返回鍵順序。
- 如果有檔案管理員把 epub 報成 `application/octet-stream`，這個 app 不會出現在「打開方式」裡。需要時再加，但加了會對所有未知檔案都出現。

## 2026-09-24（起點圓點、耳機暫停、粗斜體朗讀）

## 這次做了什麼

選取：

- 起點圓點改在字的上方（左上），終點圓點維持在字的下方（右下）。直排不變。

耳機暫停：

- 原因：Android 把耳機按鍵交給「正在發聲的 app」的媒體 session。系統語音是由 TTS 引擎自己的程序發聲，所以按鍵沒有送到本 app，而是送到上一個播過音樂的 app。
- 做法：朗讀播放時，`MainActivity` 用 `AudioTrack` 播一段循環的靜音（`epub_reader/audio` 頻道的 `silence`）。暫停或停止時關掉。`speech_media.dart` 的 `show` 和 `idle` 負責開關。

粗體、斜體朗讀：

- `sentences.dart` 在切句時記下每句的粗體、斜體範圍（`Sentence.runs`）。算粗體：`b`、`strong`、class 含 `bold`、inline `font-weight` 600 以上。算斜體：`i`、`em`、class 含 `italic`、inline `font-style: italic/oblique`。標題本身的粗體不算。
- 粗體讀慢一點（0.8 倍）、音調稍低（0.9）。
- 斜體用同一語言的第二把聲（`pickSecondVoice`，不同講者、離線優先），音調稍高（1.15）。手機只有一把聲時，只靠音調分別。
- 一句裡有不同樣式時，分段逐段讀。暫停後繼續，仍然從停下的字開始。

測試：`sentences_test.dart` 新增兩個測試。`flutter analyze` 沒有問題，全部測試通過。

Samsung SSD 沒有掛上，所以沒有建 APK，Kotlin 部分未經編譯。

## 下一步

- SSD 掛上後跑 `scripts/build-android.sh`，在實機上試：耳機暫停／繼續、藍牙耳機、起點圓點位置、粗體斜體的聲音分別。
- 分段讀可能在粗體前後有短停頓。聽起來太斷的話再調。

## 2026-09-24（英文點查、已隱藏頁）

## 這次做了什麼

點查：

- 長按英文字打開點查。最上面一條不捲動：字、繁中解釋（最多三行）、發音鍵、關閉。下面可以捲動：完整中文解釋（按詞性分行）、中文維基、英文解釋（附例句）、英文維基、資料來源。
- 中文先查內建字典 `assets/dict/word.csv`，從 `/mnt/nas4/ssd/eng/quick-read/public/word.csv` 原封不動複製過來（約 3.3MB、36,323 字）。第一次點查時在背景 isolate 讀入。變化形用 `lemmas` 欄找原形（went → go），再試常見字尾（stopping → stop、studies → study）。原形不同時寫「原形 go」，有 CEFR 等級也顯示。
- 內建字典沒有中文時，查英文維基的語言連結，找到中文條目，再用 `zh-hk` 要摘要，所以是香港繁體（例：歐拔·愛因斯坦、倫敦）。
- 英文解釋用 Wiktionary REST API，回應約 0.06 秒。原本試的 `dictionaryapi.dev` 要約 19 秒，有時回 522，所以不用。
- 沒有網路時，只顯示內建字典，並寫「連不上網路」。
- 發音鍵用系統語音、英式發音（en-GB，優先離線語音）。朗讀正在播放時，先暫停朗讀再讀字。
- 設定頁新增「點查時讀出字音」：用耳機時（預設）、每次、不讀。耳機由 `MainActivity` 的 `epub_reader/audio` 頻道回報：有線、USB、藍牙都算，所以藍牙喇叭也算。
- 長按時 `don’t` 這類彎引號的字不再被切開。

設定：

- 「已隱藏」只顯示幾本。按下去開新的一頁（`hidden_books_page.dart`），逐本恢復。

測試：新增 `test/lookup_test.dart`、`test/lookup_sheet_test.dart`。`flutter analyze` 沒有問題，40 個測試全過。網上查詢另外用臨時測試實際連過 Wiktionary 和維基（went、Paris、Einstein、London），測完已刪。

Samsung SSD 沒有掛上，所以沒有建 APK。

## 下一步

- SSD 掛上後跑 `scripts/build-android.sh`，在實機上試：點查上方那一條在小螢幕上會不會太高、en-GB 語音是否已安裝、接耳機時自動讀音、藍牙耳機的判斷。
- 若手機沒有 en-GB 語音，Android 會改用其他英文語音。要不要在點查裡提示，再決定。

## 2026-09-24（字元級劃線、搜尋、捲軸、標籤）

## 這次做了什麼

閱讀頁：

- 劃線的兩顆圓鈕改成以字為單位。長按仍先選中整句，之後拖圓鈕可以縮到只剩一個字，也可以跨句、跨段。存下的劃線記住句子和句內第幾個字（`startOffset`、`endOffset`）。舊的劃線沒有這兩個值，照舊畫整句。
- 選取後的按鈕：複製、劃線、搜尋、關閉。按搜尋後換成 Google、維基百科、書中搜尋、返回。中文字去中文維基（`/zh-hk/`），其他去英文維基。書中搜尋打開搜尋面板並直接搜。Google 和維基用外部瀏覽器（新加 `url_launcher`，manifest 加了 https 的 `queries`）。
- 百分比移到左下，避開 Android 的 home bar。點一下打開捲軸，捲軸一直開著，直到點內文。捲軸上點一下或拖動，直接跳到那個位置；手指在捲軸上時，上方顯示那個位置的章名。靠近某章開頭 10px 內會吸過去，直接打開那章第一頁。按住百分比左右拖仍然可以。
- 上方書籤鍵改成「放書籤」；目前這頁就是書籤所在時變「抽起書籤」，按下刪掉。換版面後書籤跟著它那一句更新頁數。
- 「放書籤」左邊加「加標籤」。彈出改名框：有選取時預設用選取的字，否則用這頁的劃線，再否則用章名。
- 劃線面板上方分「劃線」和「標籤」兩頁。點一筆跳過去，並先放臨時書籤。長按標籤可以改名或刪除。
- 新增 `test/marks_test.dart`。`flutter analyze` 沒有問題，35 個測試全過。選取的 JS 在瀏覽器裡用模擬觸控跑過：整句縮到一個字、跨句拖、粗體裡的字都正確。

這一輪 Samsung SSD 沒有掛上，所以沒有建 APK。

## 下一步

- SSD 掛上後跑 `scripts/build-android.sh`，在實機上試：圓鈕拖到單一字的手感、直排的圓鈕、捲軸吸附章頭的距離（10px）、章名泡泡的位置。
- 「加標籤」「標籤」的字樣在 `assets/i18n/strings.csv`，要改成別的叫法只改那裡。

## 2026-09-24（沙盒快取移到 SSD）

## 這次做了什麼

查 `/tmp/cursor-sandbox-cache` 為什麼把 Cursor 撐到當機，並改到 SSD。

- `/tmp` 是 tmpfs。沙盒把 Gradle 快取放在 `/tmp/cursor-sandbox-cache`，當時約 5.3GB，幾乎全是 Gradle。
- 已把那份快取搬到 Samsung SSD：`/run/media/hocti/92C4A6B3C4A698CD/cursor-sandbox-cache`（約 5.2GB）。`/tmp/cursor-sandbox-cache` 現在是指向那裡的符號連結。`/tmp` 從用掉 5.3GB 降到約 1.2GB。
- `scripts/sandbox-cache-on-ssd.sh` 負責建立這個連結。磁碟沒掛上就不動 `/tmp`。
- `scripts/build-android.sh` 會先跑那個腳本，並把 `GRADLE_USER_HOME` 設到同一顆 SSD。
- 使用者的 systemd path unit `cursor-sandbox-cache.path` 已啟用。登入後、那顆碟掛上時會再接一次連結。`/tmp` 重開機是空的，所以需要這個。
- 沙盒裡的指令看不到這顆 NTFS 碟。Android 建置要在沙盒外面跑，否則寫不進快取。

## 下一步

- 在實機上試：滑動翻頁的靈敏度、圓鈕大小和位置（尤其直排）、百分比捲軸拖到別章時的速度、暫停後接字（廣東話語音是否回報逐字進度）。

## 2026-09-24（v0.6）

## 這次做了什麼

閱讀頁：

- 左右滑動翻頁。向左滑下一頁，向右滑上一頁；直排相反。
- 朗讀列開著時，點一句就立刻改讀那一句。點畫面不翻頁，只有滑動翻頁。點到沒有字的地方叫出上下列。直接點中連結仍然跳過去。
- 版面只佔下半部，點上半部關閉，拿掉關閉鍵。
- 底部只顯示整本書的百分比。按住百分比再左右拖，出現捲軸；開始拖時先放臨時書籤，邊拖邊跳去預覽。
- 上下列有一條有色邊線；淺色、深色主題底色半透明，電子紙主題不透明。下方由左到右是目錄、版面、劃線、朗讀。
- 從劃線清單跳走，一定先放臨時書籤。
- 暫停再播放，從停下的那個字接著讀（用 `flutter_tts` 的逐字進度，Android 8 以上）。
- 朗讀列加了上一句、下一句的小按鈕。
- 目錄改用書本身的導覽文件（`properties="nav"`），層次照書裡的巢狀清單；沒有就用 `toc.ncx`；兩者都沒有才用舊的 spine＋`h3`/`h4`。`勝出99%人的成癮式學習法` 現在只列 12 個頂層項目，不再混入 `p-fmatter-*` 等檔案。
- 上方的書籤鍵改成「加書籤」，把那一筆臨時書籤移到目前這一頁。
- 選好範圍後，頭尾各有一顆大圓鈕，拖它改頭或尾。
- 朗讀時一句跨頁，停在已經看得到那一句的那一頁，不再翻回前一頁。

書櫃：

- 每本書和封面都不加外框。網格封面一樣高（填滿高度，太寬時裁掉左右）。書名最多兩行，網格裡短書名也留兩行位置。拿掉百分比數字，只留進度條。

其他：

- 加了測試：導覽文件的巢狀目錄、demo 書的目錄、百分比反推章和句。`library_test` 改成不假設 demo 只有兩本書。
- 在瀏覽器用合成觸控事件試過注入的腳本：長按選句、拖頭鈕擴大範圍、朗讀時點句、左滑。
- 建置 debug APK，已裝到 RFCY81D2VGM。
- `docs/features.html` 已是 v0.6，內容和這次的做法一致，沒有再改。

## 下一步

- 在實機上試：滑動翻頁的靈敏度、圓鈕大小和位置（尤其直排）、百分比捲軸拖到別章時的速度、暫停後接字（廣東話語音是否回報逐字進度）。

## 2026-09-24（v0.5）

## 這次做了什麼

- 書櫃最上面改成一行：檢視、排序、顯示封存都是圖示，設定在最右邊。拿掉「書櫃」標題列。
- 進度改成百分比。第一次打開書時，在背景分析每章的字數和圖數，不計註釋，一張圖算 300 字。之後按這一頁第一句之前的字數估算。
- 書名、章數、封面縮圖（最寬 360 px）和每章份量存起來。檔案或資料夾沒改過，就不再打開整本書。
- 重新打開書會回到上次那一章、那一句。以前只記章，所以跳章後回書櫃再進來，會回到章首。
- 臨時書籤只有一筆，只是圖示，放在內文上方新留的 24 px 空白裡。已有書籤時再跳不更新。點書籤跳回去不放新書籤，書籤用完就消失。
- 每個主題都蓋過原書的文字顏色和底色。劃線中的範圍和朗讀中的那一句，以前在黑白主題被原書顏色規則蓋掉，現在用主題的填色，一定看得到。
- 點註釋號碼跳到註釋，比翻頁優先，手指偏一點也點得到。註釋在別的檔案也可以。
- 朗讀停止後仍記住章和句。再按朗讀時，那句還在畫面上就從那裡繼續。
- 加入 `audio_service`：系統媒體控制、耳機的播放／暫停在背景也能用，上一首／下一首是上一句／下一句。
- 搜尋做好了：搜整本書，列出章名和前後文，點一筆跳過去並框出那一句。
- 規格 `docs/features.html` 改成 v0.5。

## 下一步

- 在實機上試：耳機鍵、鎖定畫面控制、背景朗讀換章、註釋跳轉、各主題的劃線顏色。這次建置了 debug APK，但沒有連接裝置，還沒裝上去。
- 書籤按鈕的行為、點查的解釋、雲端朗讀、手錶還沒有。

## 2026-09-23

## 這次做了什麼

- 書櫃的檢視和排序分成兩組。選中用填色，不再寫「使用中」。按鈕有圓角，字小一點。設定改成右上角圖示。
- 排序多了修改日期，用檔案或資料夾的最後修改時間，新的在前。
- 封存和隱藏改成長按才出現的選單。
- 網格顯示封面。封面來自 `content.opf` 的 `<meta name="cover">`，沒有時改看 `cover-image`。
- 介面文字改到 `assets/i18n/strings.csv`，目前有繁體中文和英文。設定頁可以切換。
- 修了開書時的 XML 錯誤：閱讀腳本裡的 `&&` 放進 CDATA，章節才能繼續跑，左右緣才能翻頁、換章。
- 單張圖預設縮進一個畫面，並拿掉固定的寬 viewport，避免一打開就有左右捲軸。

## 下一步

- 雲端朗讀還沒有。
- 搜尋和書籤按鈕的行為還沒有。
- 點查的解釋還沒有。
- 手錶還沒有。

## 2026-09-23

## 這次做了什麼

- `scripts/build-android.sh` 預設改成建置 debug，並安裝到目前連接的裝置。
- `--release` 才另外建置 release。`--dl` 會建置 release，並複製到下載站。
- `README.md` 和 `AGENTS.md` 已改成同一套用法。

## 下一步

- 雲端朗讀還沒有。
- 搜尋和書籤按鈕的行為還沒有。
- 點查的解釋還沒有。
- 手錶還沒有。

## 2026-09-23

## 這次做了什麼

- 朗讀的三點選單多了來源：系統、雲端。雲端看得到，但不能選。現在仍用系統語音。
- 語言改成三個：廣東話、國語、英文。預設廣東話。會挑這支手機裡對應的系統語音。
- 規格寫進 `docs/features.html` 的朗讀一節。

## 下一步

- 雲端朗讀還沒有。
- 搜尋和書籤按鈕的行為還沒有。
- 點查的解釋還沒有。
- 手錶還沒有。

## 2026-09-23

## 這次做了什麼

- 閱讀頁改成全螢幕。一次一章，按頁翻。中間叫出上下列，4 秒後消失。
- 依 Readmoo 的 class 把這一章切成一句一陣列，句內再依標點分細段。註釋不進朗讀。
- 版面、目錄（可收合）、劃線、臨時書籤（最多三個）、系統朗讀。
- 長按中文選整句，可複製或劃線。長按英文詞打開點查：下半個畫面，只有關閉，內容是空的。
- 搜尋和書籤按鈕在，按了沒有行為。沒有書店，沒有後端。
- 規格寫進 `docs/features.html` 第 6 節。

## 下一步

- 搜尋和書籤按鈕的行為還沒有。
- 點查的解釋還沒有。
- 手錶還沒有。

## 2026-09-23

- 抽了 `demo/電玩的本質`（Readmoo，已解壓）。類型看原 class，不看 `moofs*` / `moolh*`。
- 寫了閱讀頁 prompt 底稿 `docs/plan-read-view.md`（繁中 markdown）：一句一筆的 array（含類型，句內再依標點分細段，給 TTS）、全螢幕翻頁、中間叫出上下列、版面、目錄收合、劃線、臨時書籤、長按選句、TTS 列。
- 沒有改 app，也沒有併進 `docs/features.html`。

## 2026-09-23

- 依 `docs/features.html` 寫了第二份草案 `docs/plan-2-settings.md`（繁中 markdown）。
- 這份只寫這一輪不做的設定：閱讀版面（字級、行距、左右邊界、字型、對齊）、點查的設定項、朗讀的設定項。
- 主題和版本之間的空白列維持空白，沒有代填。
- 預設下一輪只把改過的文字併進規格，不寫 app。使用者會再改這份，當成下次的 prompt。

## 2026-09-23

- 寫了基本功能表 `docs/features.html`（繁中 HTML）。狀態是草案，等使用者修改後才定案。
- 寫了 `AGENTS.md`、`README.md`、`changelog.md`、`.gitignore`。
- 放了空白 Flutter 殼（`pubspec.yaml`、`lib/main.dart`）：書櫃、閱讀、設定三個畫面，右上角進入設定，沒有動畫。沒有掃描書、沒有 WebView。
- 本機沒有 Flutter SDK，所以沒有產生 `android/`，也沒有執行這個殼。
